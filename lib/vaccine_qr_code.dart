import 'package:flutter/material.dart';

class VaccineQrCode extends StatelessWidget {
  const VaccineQrCode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}